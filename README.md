# pyproxy

## Testing

cmdl = ["/bin/echo", "-n", "8.8.8.8:53"]

## Code

if (len(r) != size):
	fout("! debug get %s %s" % (len(r), size))

while (self.x and (not self.x.endswith(b"\n"))):
            t = self.s.recvpipe(BUFF)
            if (not t):
                break
            self.x += t
